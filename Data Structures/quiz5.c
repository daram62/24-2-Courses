#include <stdio.h>
#include <stdlib.h>

struct Rectangle {
    int length;
    int width;
};

struct Triangle {
    int base;
    int height;
};

struct Circle {
    float radius;
};

int main() {
    Rectangle rect = {10, 5};
    Triangle tri = {5, 4};
    Circle cir = {7.0};

}